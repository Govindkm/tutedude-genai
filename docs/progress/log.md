# 📚 Python Learning Progress Log

This file tracks all learning sessions date-wise. Copilot reads this at the start of every session to provide a personalized warm-up and revision before introducing new content.

---

<!-- Copilot: Append new session entries below this line. Do NOT edit past entries. -->

## 📅 2026-06-04 — Session 1

**Topics Covered:** Python Lists
**Concepts Learned:**
- List creation, indexing, negative indexing: understood well ✅
- List methods — `append`, `insert`, `remove`, `pop`: understood well ✅
- `len()`, `sum()`, `min()`, `max()`: understood well ✅
- Sorting with `sorted()` (non-destructive) vs `.sort()` (in-place): understood well ✅
- `list.index(val, start)` — finding second occurrence: understood well ✅
- `zip()` — pairing two lists: introduced, used in solution ✅
- List comprehension: attempted with manual loop instead — **needs practice** 🔄
- `enumerate()`: not yet covered — **to be taught next** 📌

**Problems Attempted:**
| Problem | Difficulty | Outcome |
|---------|-----------|---------|
| Movie watchlist — add, replace, len, negative index | 🟢 | ✅ Solved |
| Movie watchlist — numbered report with loop | 🟡 | ✅ Solved (minor fix: i+1) |
| Student grade manager — 6-part challenge | 🔴 | 🔄 Partially (Tasks 1–4, 6 ✅ / Task 5 skipped — enumerate unknown) |

**Areas to Strengthen:**
- **List comprehension** — used manual `for` loop instead; needs practice with `[expr for x in list if cond]`
- **`enumerate()`** — not yet learned; needed for Task 5

**Revision Card Created:** No

**Next Session Should Cover:**
1. `enumerate()` — teach with examples
2. List comprehension practice — dedicated exercises
3. Move to **Dictionaries** after above gaps are filled
